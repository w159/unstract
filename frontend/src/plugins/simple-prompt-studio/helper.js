// Stub implementation for simple-prompt-studio helper
export const processPromptData = (data) => {
  return data;
};

export const validatePrompt = (prompt) => {
  return true;
};

export const formatPromptOutput = (output) => {
  return output;
};

export default {
  processPromptData,
  validatePrompt,
  formatPromptOutput,
};
