import PropTypes from "prop-types";

// Stub implementation for ReviewLayout component
const ReviewLayout = ({ children }) => {
  return children || null;
};

ReviewLayout.propTypes = {
  children: PropTypes.node,
};

export default ReviewLayout;
