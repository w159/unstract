import PropTypes from "prop-types";

// Stub implementation for UnstractSubscriptionCheck component
const UnstractSubscriptionCheck = ({ children }) => {
  return children || null;
};

UnstractSubscriptionCheck.propTypes = {
  children: PropTypes.node,
};

export default UnstractSubscriptionCheck;
