// Stub implementation for llm-whisperer helper
export const processWhisperData = (data) => {
  return data;
};

export const validateWhisperInput = (input) => {
  return true;
};

export const formatWhisperOutput = (output) => {
  return output;
};

export default {
  processWhisperData,
  validateWhisperInput,
  formatWhisperOutput,
};
